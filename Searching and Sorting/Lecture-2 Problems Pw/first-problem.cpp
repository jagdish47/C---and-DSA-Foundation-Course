#include <bits/stdc++.h>
using namespace std;

int main()
{

    vector<int> vec{19, 12, 23, 8, 16};
    vector<int> is_visited(vec.size(), 0);

    for (int i = 0; i < n; i++)
    {
    }

    return 0;
}
