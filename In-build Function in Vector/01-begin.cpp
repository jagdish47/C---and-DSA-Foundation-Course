#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> vec{8, 3, 43, 34, 54, 33};

    // Using the begin() function to obtain an iterator pointing to the first element
    auto it = vec.begin();

    // Printing the value of the iterator
    cout << *it << endl;

    return 0;
}
